# Copy to ~/.config/seastar/io.conf. Command-line options take precedence.
reactor-backend=io_uring

# If io_uring is unavailable or restricted, use --reactor-backend=epoll, or
# remove the setting above to let Seastar select an available backend.
# Configure io-properties-file only after measuring the actual storage with
# Seastar's iotune tool; no machine-specific disk limits are assumed here.
